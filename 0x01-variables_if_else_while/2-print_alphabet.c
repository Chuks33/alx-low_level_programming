#include <stdio.h>
/**
 * main - entry point
 *
 * Return: alway returns 0
 *
 */
int main(void)
{
char a;

for (a = a; a <= z ; a++)
putchar(a);
putchar(n);
return (0);
}
#include <stdio.h>

/**
 * main - entry point
 *
 * Return: alway returns 0
 *
 */
int main(void)
{
char a;

for (a = a; a <= z ; a++)
putchar(a);
putchar(n);
return (0);
}
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
char i;

for (i = a; i <= z; i++)
{
putchar(i);
}
putchar(n);
return (0);
}
